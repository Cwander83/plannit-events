import React from 'react';

const EventFormHeader = ({ title }) => (
	<h4 className=" border-b-2 border-secondary text-2xl pb-3 font-semibold tracking-wider uppercase">
		{title}
	</h4>
);

export default EventFormHeader;
